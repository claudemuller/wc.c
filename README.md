# wc

*nix's wc recreated in C.

## Building

```bash
make
```

## Running

```bash
$ ./bin/wc

usage: wc [mode] <file>
        Modes:
                -c number of bytes
                -l number of lines
                -w number of words
                -m number of characters
```

